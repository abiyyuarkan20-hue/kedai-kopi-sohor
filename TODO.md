# TODO - Search Functionality Implementation

## Plan:

1. [x] Update `src/app.js` - Add searchQuery property and filteredItems getter
2. [x] Update `index.html` - Add x-model on search input and use filteredItems in template
3. [x] Test the search functionality

## Progress:

- [x] Step 1: Modify src/app.js
- [x] Step 2: Modify index.html
- [x] Step 3: Search functionality implemented

## Summary:

- Added `searchQuery` property and `filteredItems` getter in `src/app.js`
- Added `x-model="searchQuery"` on search input in `index.html`
- Changed menu template to use `filteredItems` instead of `items`
